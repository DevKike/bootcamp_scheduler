server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/technologies_microservice
    username: ${username}
    password: ${password}
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update